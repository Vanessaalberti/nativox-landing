# servidor — landing

- `index.ts` — sirve el sitio.
- `prueba-nube.ts` *(opcional)* — prueba en la nube con Workers AI de la cuenta de Vanessa: límite por visitante (binding de rate limiting) y cuota diaria. Sin secretos en el repositorio.
