# comparacion — datos de la comparación

| Carpeta | Qué tiene |
| --- | --- |
| `charlas/` | Lista de charlas usadas (link, fragmento, idioma, licencia) |
| `referencias/` | Transcripción revisada y glosario de cada fragmento |
| `resultados/` | Resultados por combinación (JSON), generados con el script de la aplicación (`scripts/informe-calidad`) |

Método: `documentacion/metodo-comparacion.md`.
