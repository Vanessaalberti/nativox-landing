# navegador — landing

`arranque/` (arranque) · `rutas/` (páginas finas) · `funcionalidades/` (una carpeta por sección) · `interfaz/` (sistema de diseño) · `copia-modulos/` (copia sincronizada de módulos de la aplicación; no se edita).
